<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.hello.mapper.MenuMapper">
    
    <resultMap id="BaseResultMap" type="com.example.hello.entity.Menu">
        <id column="id" property="id"/>
        <result column="menu_code" property="menuCode"/>
        <result column="menu_name" property="menuName"/>
        <result column="parent_id" property="parentId"/>
        <result column="menu_type" property="menuType"/>
        <result column="menu_url" property="menuUrl"/>
        <result column="menu_icon" property="menuIcon"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="status" property="status"/>
        <result column="remark" property="remark"/>
        <result column="created_time" property="createdTime"/>
    </resultMap>
    
    <!-- 根据角色 ID 查询菜单权限 -->
    <select id="selectMenusByRoleId" resultMap="BaseResultMap">
        SELECT m.*
        FROM tb_menu m
        INNER JOIN tb_role_menu rm ON m.id = rm.menu_id
        WHERE rm.role_id = #{roleId}
          AND m.status = 1
        ORDER BY m.sort_order ASC
    </select>
    
    <!-- 根据角色编码查询菜单权限 -->
    <select id="selectMenusByRoleCode" resultMap="BaseResultMap">
        SELECT DISTINCT m.*
        FROM tb_menu m
        INNER JOIN tb_role_menu rm ON m.id = rm.menu_id
        INNER JOIN tb_role r ON rm.role_id = r.id
        WHERE r.role_code = #{roleCode}
          AND m.status = 1
        ORDER BY m.sort_order ASC
    </select>
    
    <!-- 查询所有菜单 -->
    <select id="selectAllMenus" resultMap="BaseResultMap">
        SELECT * FROM tb_menu WHERE status = 1 ORDER BY sort_order ASC
    </select>
    
    <!-- 根据菜单编码查询菜单 -->
    <select id="selectByMenuCode" resultMap="BaseResultMap">
        SELECT * FROM tb_menu WHERE menu_code = #{menuCode} LIMIT 1
    </select>
    
    <!-- 检查角色是否有菜单权限 -->
    <select id="hasMenuPermission" resultType="boolean">
        SELECT COUNT(1) > 0
        FROM tb_role_menu rm
        INNER JOIN tb_menu m ON rm.menu_id = m.id
        WHERE rm.role_id = #{roleId}
          AND m.menu_code = #{menuCode}
          AND m.status = 1
    </select>
    
</mapper>
